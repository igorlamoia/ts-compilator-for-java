const iterator = {
  unexpected_token:
    'Unexpected token at line {line}, column {column}. Expected type {expectedType}, but got type {actualType}, lexeme "{lexeme}".',
  no_more_tokens: "No more tokens available.",
};

export default iterator;
