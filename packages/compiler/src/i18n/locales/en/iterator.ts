const iterator = {
  unexpected_token:
    'Unexpected token at line {line}, column {column}. Expected "{expectedType}", but got "{actualType}" (lexeme "{lexeme}").',
  no_more_tokens: "No more tokens available.",
};

export default iterator;
